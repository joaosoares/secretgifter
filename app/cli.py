from main.py import *
import argparse

if __name == '__main__':

    #create the parser
    parser = argparse.ArgumentParser(
            description="Manages a Secret Santa draw and sends SMS to participants")

    # add the arguments
    parser.add_argument(
            '')
